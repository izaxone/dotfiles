general {
        output_format = "i3bar"
        colors = true
        interval = 5
}

order += "load"
order += "disk /"

load {
    format = "[ load: %1min, %5min, %15min ]"
}
disk "/" {
    format = "%avail"
}
